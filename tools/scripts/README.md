# tools-scripts

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build tools-scripts` to build the library.

## Running unit tests

Run `nx test tools-scripts` to execute the unit tests via [Jest](https://jestjs.io).
